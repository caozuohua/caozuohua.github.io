---
title: "1GB VPS 上跑个人助理智能体的部署清单"
date: 2026-07-07
publishDate: 2026-07-07
description: "整合 Hermes-lite 裁剪和 VPS 安全经验，给出一份 1GB VPS 上部署个人助理智能体的前中后检查清单。"
tags: ["AI Agent", "Hermes-lite", "个人助理", "QPC", "Skill Routing"]
categories: ["个人助理智能体"]
draft: false
---

1GB VPS 可以跑个人助理智能体，但前提是别把它当大服务器用。

低资源环境最重要的是克制：少进程、少常驻、少浏览器、少后台任务。部署前就要想清楚哪些功能必须在线，哪些功能可以按需运行。

## 部署前检查

先确认资源边界：

- 内存是否约 1GB。
- 是否有 swap。
- 磁盘空间是否足够日志和依赖。
- 系统是否开启自动安全更新。
- SSH 登录是否使用密钥。
- 防火墙是否只开放必要端口。

如果机器本身不稳，不要急着部署 Agent。

## 功能裁剪

个人助理在 1GB VPS 上应该优先保留核心链路：

- 消息入口。
- 模型网关或 API 调用。
- Skill 路由。
- 少数必要工具。
- 轻量记忆或知识库。
- 日志。

可以延后或禁用：

- 浏览器自动化常驻服务。
- 大型向量数据库。
- 多 Agent 并发编排。
- 语音转写和合成。
- 重型监控面板。

功能越少，系统越容易稳定。

## 部署中检查

部署时按顺序验证：

1. 服务能启动。
2. 入口能收到消息。
3. 模型调用能返回。
4. Skill 路由能命中。
5. 工具调用能成功。
6. 记忆写入和读取正常。
7. 日志能落盘。
8. 服务重启后状态可恢复。

不要等全部部署完才第一次测试。

## 安全检查

个人助理通常握有 API Key 和个人数据，安全边界要提前做。

检查项：

- 密钥不写进仓库。
- 配置文件权限收紧。
- 面板不直接暴露公网。
- 反向代理路径不可预测。
- 高风险工具默认关闭。
- 日志不打印完整密钥。
- 远程命令需要人工确认。

低资源不是降低安全要求的理由。

## 部署后检查

上线后观察 24 小时：

- 空闲内存。
- 峰值内存。
- 服务是否自动重启。
- 日志是否快速膨胀。
- API 调用是否异常。
- 是否有失败请求未处理。

如果内存长期紧张，优先继续裁剪功能，而不是堆更多守护进程。

## 最小维护命令

维护手册里至少保留这些动作：

```text
查看服务状态
查看最近日志
重启服务
备份配置
恢复配置
检查磁盘空间
检查开放端口
```

命令本身可以因系统而异，但动作必须明确。

## 低资源部署原则

1GB VPS 上的个人助理，目标不是展示完整能力，而是 24/7 稳定承担少数关键任务。

如果某个功能让内存、权限和故障排查成本显著上升，就应该先问：它是否真的服务日常需求。
